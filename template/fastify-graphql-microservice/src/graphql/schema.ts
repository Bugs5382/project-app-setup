// @ts-nocheck
import { gql } from 'mercurius-codegen'

const schema = gql`
    extend type Mutation {
        // insert resolvers
    }
          
    extend type Query {
        // insert resolvers
    }
`

export default schema
