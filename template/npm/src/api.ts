/**
 * Use to export TypeDoc into Different Namespaces
 * Example:
 *
 * export * as CORE from './index.js' // depending on the package, this might not be needed
 * export * as OTHER from './other.js' // depending on the package, this might not be needed
 *
 * Otherwise, it needs to follow TypeDoc's API generation Examples.
 *
 */
